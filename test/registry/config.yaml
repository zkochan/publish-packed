---
storage: ./storage
packages:
  '*':
    access: $all
    publish: $all
    proxy: npmjs
uplinks:
  npmjs:
    url: https://registry.npmjs.org/
logs:
  - {type: stdout, format: pretty, level: error}
